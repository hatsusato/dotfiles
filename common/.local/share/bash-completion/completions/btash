_btash() {
	local cur prev words cword

	_init_completion || return
	COMPREPLY=()

	if [[ ${COMP_CWORD} -eq 1 ]]; then
		COMPREPLY=($(compgen -W 'menu new list clean' -- "${cur}"))
	fi
}

complete -F _btash btash
