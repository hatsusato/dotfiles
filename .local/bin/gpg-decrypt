#!/bin/bash
# apt: gpg

set -eu

usage() {
  cat <<EOF >&2
USAGE: $0 ENCRYPTED
    print decrypted contents to stdout
EOF
  exit 1
}
main() {
  gpg --output - --decrypt "$1"
}

(($# == 1)) || usage
main "$@"
