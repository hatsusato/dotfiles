#!/bin/bash

set -eu

main() {
  local line conf=$HOME/.config/local/.ensure-dirs
  [[ -f $conf ]] || touch "$conf"
  while read -r line; do
    [[ $line == '#'* ]] && continue
    eval mkdir -m700 -p "$line"
  done <"$conf"
}

main
