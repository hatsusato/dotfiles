#!/bin/bash

set -eu

run() {
  "$@" &>/dev/null || :
}

mkdir -p "$HOME"/{sshfs,Private}
mkdir -m700 -p "/tmp/$USER"/{,Downloads}
run man-env
run xkb-load
pgrep xkb-monitor >/dev/null || run xkb-monitor &
