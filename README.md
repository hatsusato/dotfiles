# dotfiles

Personal dotfiles deployment system for Linux / WSL / Git Bash on Windows.

## Development Setup

```bash
pip install pre-commit
pre-commit install
```

This installs Git hooks that run shellcheck, shfmt, and line-ending checks on every commit.
